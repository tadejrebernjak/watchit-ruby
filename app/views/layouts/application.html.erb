<!DOCTYPE html>
<html>
  <head>
    <title>Watchit</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="row navbar navbar-dark bg-dark d-flex flex-row flex-wrap align-items-center justify-content-between p-4">
      <%= link_to root_path, class: 'header-logo col-1 m-4 text-light text-center' do %>
        <h1>WatchITonRails</h1>
      <% end %>
      <div class="header-links col-4 d-flex justify-content-end align-items-center">
        <%= display_session_links %>
      </div>
    </header>
    <div class="container p-3">
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
      <%= yield %>
    </div>
    <footer>
    
    </footer>
  </body>
</html>
